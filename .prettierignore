docs/research/proximal_distal_energy_transfer/data/articulated_crba_requalification_report.json
docs/research/proximal_distal_energy_transfer/data/articulated_drift_contact_attribution.json
docs/research/proximal_distal_energy_transfer/data/articulated_manufactured_solution.json
docs/research/proximal_distal_energy_transfer/data/articulated_native_constraint_discrepancy.json
# Repository_Management#1520: SPEC.md's change-log table is left unpadded on
# purpose. Prettier pads markdown table cells to the width of the widest row,
# so a single new change-log row would rewrite every other row -- turning a
# one-line addition into a whole-table diff that conflicts with every
# concurrent pull request. That is the exact failure #1520 removes.
#
# The table also carries <!-- prettier-ignore-start/end --> markers, which are
# the narrower mechanism and are sufficient on their own in Tools. They are NOT
# sufficient here: with them in place `prettier --check SPEC.md` still reports
# this file. The acceptance test is `prettier --check SPEC.md`, not the
# presence of either mechanism.
SPEC.md

# UpstreamDrift#9474: docs/shared_tools/divergence_inventory.md is generated by
# `python -m scripts.shared_tools.divergence_inventory --write` and is checked
# against a 50 KiB doc size budget. Prettier pads markdown table cells to the
# widest row, which inflates the generated 43 KB file to 70 KB and fails
# `scripts/check_doc_size_budget.py`. The two gates cannot both be satisfied,
# so the generator's canonical output wins -- same rule as
# docs/development/feature_parity_matrix.md.
docs/shared_tools/divergence_inventory.md
