# Next Agent Instructions

## Transfer Summary

Repository: `D-sorganization/UpstreamDrift`.
Planning worktree: `C:/Users/diete/Repositories/.worktrees/upstream-gspro-10188`.
Branch: `docs/issue-10188-gspro-integration`.
Baseline: `395d3de876d8f87b7b00785ef2f430d0e9387ce7`.
Delivery commit: `SELF` means the commit containing this planning packet;
resolve with `git rev-parse HEAD`. Obtain the planning PR/current state from
[AGENT_HANDOFF.md](../../../AGENT_HANDOFF.md) and the parent epic.

Objective: use GSPro to display shots generated by UpstreamDrift models,
initially with a synchronized companion golfer view; keep destinations
interchangeable. Epic #10188 and children #10189–#10200 are open. This delivery
contains a source-backed review, proposed architecture, detailed GitHub work
packages and a delegated acceptance runbook. It contains **no implementation**
of the proposed outbound client, session service or codec.

The original checkout at `C:/Users/diete/Repositories/UpstreamDrift` has another
active branch and untracked user/agent work (`_wt_claude_base/`, `scratch/`,
`sessions/`, `ui/src/api/vite-proxy-config.test.ts`). Leave it untouched. Use a
new child worktree from current main once the planning PR lands; do not branch
implementation from an unrelated active checkout.

## Must-Read Context

1. Repository `AGENTS.md`, `CLAUDE.md`, `SPEC.md` and canonical handoff.
2. [Feasibility](FEASIBILITY.md), [Architecture](ARCHITECTURE.md),
   [Backlog](BACKLOG.md), [Acceptance Runbook](ACCEPTANCE_RUNBOOK.md).
3. `docs/agents/shared-infrastructure.md` and `docs/agent_context/USAGE.md`.
4. Public physics, impact, launch, visualization and contract facades cited in
   the feasibility review; inspect source again if the baseline changed.
5. The exact GitHub child before claiming it. GitHub is task authority.

## What the Lead Has Already Resolved

- Separate numerical flight, shot delivery and golfer presentation contracts.
- One canonical immutable shot with SI/angular vectors, trace identity and
  explicit source/qualification; GSPro conversions live at the adapter edge.
- Companion replay is achievable scope. Native GSPro animation and autonomous
  course feedback remain vendor-gated research.
- Intent persistence before writes, uncertain-delivery state, no automatic shot
  replay and no exactly-once claim. Status success requires verified attribution.
- Local reference adapter reuses existing ball-flight/rendering infrastructure.
- Keep raw GSPro socket local; remote models use an authenticated application bridge.
- Scientific defects have dedicated GS-04 scope; a peak-speed sample is a demo.

## Work Still Reserved for the Lead

1. GS-00 #10189: actual license/version confirmation and protocol observations,
   including units, signs, framing, heartbeat and acknowledgment correlation.
2. GS-03 #10192: concurrency, durable intent, crash recovery, cancellation,
   ambiguous responses and transport lifecycle.
3. GS-04 #10193: preserving full impact state and contact/scientific qualification.
4. Review profile/contract changes and live acceptance, and conduct optional
   vendor research. No worker should invent a convention to unblock these.

## First Worker Assignment: Canonical Domain Slice

**Issue:** GS-01 #10190. This slice can start before live GSPro is available.
It requires no vendor-specific field interpretation.

1. From `Repository_Management`, run the current claim checker for issue 10190
   and post a lease if free. Use the current session ID and approved agent ID.
2. Create a focused `feat/issue-10190-shot-contracts` branch/worktree. Check for
   an existing scoped PR before creating one.
3. Write failing tests for immutable shot vectors, identities, schema version,
   finite numbers, replay time bounds and capabilities. Save red-test output.
4. Implement only contracts/facade and a minimal public conversion from existing
   launch/post-impact results. Preserve angular vectors and explicit provenance.
   Do not infer spin-axis wire signs, invent engine contact or open a socket.
5. Verify invalid values still fail under optimized Python and disabled optional
   DbC diagnostics. Mutable inputs must not mutate stored shot values.
6. Run narrow tests/lint/format/type checks; update canonical handoff and DL-#10188.
7. Request lead review of the public contract and independent numerical goldens.
   The contract needs frozen field names/types before dependent code starts.

**Own:** proposed `src/shared/python/golf_simulator/contracts.py`, curated facade,
small conversion seam, matching `tests/unit/golf_simulator/` tests, and required
state documents. **Exclude:** transport, journal, GUI, physics solver changes,
vendored files, workflow changes and runtime vendor profiles.

## Second Worker Assignment: Pure Codec

**Issue:** GS-02 #10191, only after GS-00 and GS-01 are reviewed.
Lead must hand over the profile version/digest, independently verified units and
sign fixtures, optional-field rules, loss tolerance and response categories.

Write failing exact-payload tests, implement codec functions and run green
tests. Inputs include straight, left/right, zero spin, putt, chip and invalid
vectors. Expected payloads must not call production conversion code. Keep
unknown/unverified protocol behavior explicit and return it to the lead.
Own only codec plus tests/fixtures and required handoff/log updates.

Do not independently "fix" acknowledgment behavior, add retries, contact the
vendor or start live submission. The first coding assignment's successful tests
do not qualify the second assignment's vendor profile.

## Later Easy Work

After service/replay contracts land, delegate local-reference adapter wiring,
thin API/UI controls, capability presentation and runbook execution. Each worker
gets a single child, allowed files, exact dependency commits and acceptance
tests. The current sub-agent has already completed the bounded repository
survey and acceptance-runbook draft; it has not been left running on an
unbounded implementation task.

## Validation Commands

Existing commands for the planning packet:

```powershell
python3 scripts/check_document_title_case.py docs/plans/golf_simulator_integration/README.md docs/plans/golf_simulator_integration/FEASIBILITY.md docs/plans/golf_simulator_integration/ARCHITECTURE.md docs/plans/golf_simulator_integration/BACKLOG.md docs/plans/golf_simulator_integration/NEXT_AGENT.md docs/plans/golf_simulator_integration/ACCEPTANCE_RUNBOOK.md
python3 scripts/ci/check_spec_changelog_duplicates.py
git diff --check
```

The documented `shared_scripts/development_log.py` is absent in this checkout.
Use the authoritative checker from a local `Repository_Management` clone:
`python3 C:/Users/diete/Repositories/Repository_Management/shared_scripts/development_log.py --repo-root .`.
Initial full-log validation found pre-existing missing commit SHAs in
DL-#10003 and DL-#10062. DL-#10188 records its verified source baseline explicitly.
Report those existing findings rather than rewriting another agent's evidence.

Planning validation on 2026-09-15: all applicable pre-commit checks passed
(formatting guidance, document title capitalization, design-manual governance
and Prettier); Python-only checks skipped because no Python changed. Six plan
documents passed title case; all 12 local plan links resolved; SPEC duplicate
and whitespace checks passed. Comparing development-log validation against
the source baseline found the same two findings and zero new findings. No
model, network or licensed live test was run; none is implied by these results.

Future implementation commands, **not runnable as acceptance until those test
paths are created**:

```powershell
python3 -m pytest tests/unit/golf_simulator -n auto --timeout=60
python3 -m pytest tests/integration/golf_simulator -n auto --timeout=60
python3 -m ruff check src/shared/python/golf_simulator tests/unit/golf_simulator tests/integration/golf_simulator
python3 -m ruff format --check src/shared/python/golf_simulator tests/unit/golf_simulator tests/integration/golf_simulator
```

Add targeted mypy and the repo-required full gates according to changed areas.
Physics changes run affected existing impact/pipeline tests and the design-manual
governance command after reading its configuration. UI changes update and test
feature parity and launcher embedding. Live tests need an explicit opt-in marker
and valid local GSPro setup; default CI must not start licensed software.

## Required Return Package

- Child, branch, resolved commit SHA and PR URL/state.
- Files changed and which acceptance items are actually satisfied.
- Exact red/green/regression/gate commands and exit status; include real failures.
- Contract/profile versions, fixture provenance and retained assumptions.
- Remaining work and one concrete next action; no broad "all done" claim.
- Canonical handoff plus refreshed DL-#10188 in the same implementation commit.

Keep feature issues open until their implemented criteria are proven. A docs PR
does not close #10188. No simulated/visual flight evidence substitutes for
contact/scientific qualification, and no generic network response substitutes
for a vendor-attributable acknowledgment.
