name: sim-env
channels: [conda-forge, defaults]
dependencies:
  - python=3.11
  - numpy
  - pandas
  - matplotlib
  - scipy
  - pip
  - pip:
      - pytest
      - pyyaml
      - defusedxml>=0.7.1
      - structlog>=24.0.0
      - colorama>=0.4.6
