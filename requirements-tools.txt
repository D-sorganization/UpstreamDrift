# Shared Tools source dependency; kept separate from PyPI package metadata.
# Install with: python3 -m pip install -r requirements-tools.txt
# Keep this revision equal to vendor/ud-tools and Cargo.toml. The required
# agent-context job rejects divergent wheel/source/Rust installation paths.
ud-tools @ git+https://github.com/D-sorganization/Tools.git@e83bd2e4a7a29a2dcd8145ef2d1efa07123324f0
