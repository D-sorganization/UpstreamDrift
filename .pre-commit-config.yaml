# =============================================================================
# Fleet-Wide Pre-commit Configuration
# =============================================================================
# Fast checks on every commit (<15 seconds)
# Slower checks (mypy, bandit, tests) on pre-push only
#
# EXCEPTIONS:
# - Documentation-only changes (.md, .rst) skip Python checks
# - Workflow changes (.github/) skip Python checks
# - Config changes (.yml, .yaml, .json) only run prettier
#
# Installation:
#   pip install pre-commit
#   pre-commit install
#   pre-commit install --hook-type pre-push
# =============================================================================

repos:
  # -------------------------------------------------------------------------
  # Stage 1: Code Formatting (auto-fix) - PYTHON FILES ONLY
  # -------------------------------------------------------------------------

  # Ruff - Fast Python linter with auto-fix
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.17
    hooks:
      - id: ruff
        name: "ruff (lint + fix)"
        args: ["--fix", "--exit-non-zero-on-fix"]
        types: [python]
        # Skip for docs-only or workflow-only changes
        exclude: ^(\.github/|docs/|.*\.md$)

      - id: ruff-format
        name: "ruff format"
        types: [python]
        exclude: ^(\.github/|docs/|.*\.md$)

  # Guard for formatter guidance consistency (Makefile/docs/pre-commit)
  - repo: local
    hooks:
      - id: formatter-guidance-consistency
        name: "formatter guidance consistency (ruff)"
        entry: python scripts/check_formatter_guidance.py
        language: system
        pass_filenames: false
        always_run: true

  # -------------------------------------------------------------------------
  # Stage 2: Quick Quality Checks - PYTHON FILES ONLY
  # -------------------------------------------------------------------------

  - repo: local
    hooks:
      # Prevent wildcard imports
      - id: no-wildcard-imports
        name: "no wildcard imports"
        entry: "from .* import \\*"
        language: pygrep
        types: [python]
        exclude: ^(archive/|legacy/|experimental/|tests/|\.github/)

      # Check for debug statements
      - id: no-debug-statements
        name: "no debug statements"
        entry: "(?:^|\\s)(breakpoint\\(|import pdb|pdb\\.set_trace)"
        language: pygrep
        types: [python]
        exclude: ^(tests/|archive/|legacy/|\.github/)

      # Check for print statements in src (should use logging)
      - id: no-print-in-src
        name: "no print() in src (use logging)"
        entry: "^\\s*print\\("
        language: pygrep
        types: [python]
        files: ^src/
        exclude: ^(src/.*/__main__\.py|tests/|\.github/|src/shared/python/codemap/(cli|watcher|mcp_server)\.py$)

      - id: document-title-capitalization
        name: "document title capitalization"
        entry: python scripts/check_document_title_case.py --staged
        language: python
        additional_dependencies: [pypdf>=5.0]
        files: '\.(md|qmd|tex|docx|pdf)$'

      - id: design-manual-governance
        name: "engineering design-manual governance"
        entry: python -m scripts.check_design_manual_governance
        language: system
        pass_filenames: false
        files: ^(manuals/upstreamdrift/|scripts/config/design_manual_governance\.json$|scripts/(check_design_manual_governance|design_manual_contract)\.py$|AGENTS\.md$|CLAUDE\.md$|SPEC\.md$|AGENT_HANDOFF\.md$)

  # -------------------------------------------------------------------------
  # Stage 3: Config File Formatting - ALWAYS RUNS
  # -------------------------------------------------------------------------

  # Prettier - Format YAML, JSON, Markdown (lightweight, always runs)
  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v3.1.0
    hooks:
      - id: prettier
        name: "prettier (yaml/json/md)"
        types_or: [yaml, markdown, json]
        # These scientific/feature artifacts are generated byte-for-byte and
        # freshness-checked in CI; Prettier rewrites their canonical output.
        exclude: '(\.py|\.m|package-lock\.json|\.ipynb)$|^docs/development/feature_parity_matrix\.md$|^docs/shared_tools/divergence_inventory\.md$|^docs/research/proximal_distal_energy_transfer/(release_manifest\.json|data/(advanced_biological_bridge|articulated_contact_projection|articulated_distributed_grip_atlas|articulated_forward_contact|articulated_inertia_cross_engine|articulated_slack_atlas|claim_adjudication_summary|claim_audit_registry|claim_candidate_inventory|claim_evidence_manifest|claim_numeric_contracts|closed_state_forward_bridge|counterfactual_ensemble|event_topology_channel_matrix|event_topology_robustness|event_topology_stress_extension|external_source_review|forward_contact_validity_horizon|forward_two_arm_study|local_linear_diagnostics|mechanism_ladder_study|moving_base_flexible_study|moving_base_modal_shaft_study|nonlinear_controller_comparison_registration|nonlinear_controller_plant_transport|nonlinear_controller_solver_qualification|phase_event_stability|release_claim_review|results_chapter_audit|shaft_contribution_study|spatial_forward_contact_study|spatial_full_body_study|torque_allocation_preload_study|trajectory_control_authority|transmission_robustness_study|transmission_stability_audit|two_hand_wscg_analysis|uncertainty_control_stability_audit|uncertainty_control_study)\.json)$'

  # -------------------------------------------------------------------------
  # Stage 4: Pre-push Hooks (Slower - PYTHON FILES ONLY)
  # -------------------------------------------------------------------------

  # MyPy - Type checking (pre-push only, Python only)
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.13.0
    hooks:
      - id: mypy
        name: "mypy (type check)"
        stages: [pre-push]
        additional_dependencies:
          - types-requests
          - types-PyYAML
          - pydantic
        args: [--config-file, pyproject.toml, --follow-imports=silent]
        types: [python]
        files: ^src/
        exclude: |
          (?x)^(
            src/matlab/|
            src/javascript/|
            src/archive/|
            src/legacy/|
            src/experimental/|
            src/shared/python/pendulum_simulator/|
            src/launchers/|
            src/engines/physics_engines/mujoco/python/|
            src/engines/physics_engines/drake/python/src/|
            src/engines/physics_engines/pinocchio/python/pinocchio_golf/|
            src/bunkershot3d/backends/chrono/|
            src/shared/python/humanoid_character_builder/generators/|
            src/shared/python/assessment/|
            src/shared/models/opensim/|
            src/tools/starting_pose_matcher/|
            src/shared/python/data_io/export\.py|
            src/shared/python/data_io/marker_mapping\.py|
            src/engines/common/export\.py|
            src/engines/Simscape_Multibody_Models/|
            __pycache__/|
            \.pytest_cache/|
            \.venv/|
            \.github/
          )

  # Bandit - Security scanning (pre-push only, Python only)
  - repo: https://github.com/PyCQA/bandit
    rev: 1.7.7
    hooks:
      - id: bandit
        name: "bandit (security scan)"
        stages: [pre-push]
        args: ["-r", "src/", "-lll", "-ii"]
        pass_filenames: false
        # Only run if Python files changed
        types: [python]

  # Pytest - Unit tests (pre-push only)
  - repo: local
    hooks:
      - id: pytest-unit
        name: "pytest (unit tests)"
        stages: [pre-push]
        # NOTE: Many tests/unit/ files have state-pollution failures when run
        # as part of the full suite (engine loaders, third-party audit, isolated
        # subprocess tests).  Scope pre-push to subdirectories that are reliably
        # self-contained and hardware-independent; full suite runs in CI.
        entry: python -m pytest
          tests/unit/dbc
          tests/unit/core
          tests/unit/utils
          -x -q --tb=short -m "not slow and not integration"
          -p no:xvfb
          --override-ini="addopts=-ra -q --strict-markers --strict-config --durations=20"
        language: system
        pass_filenames: false
        # Only run if Python files changed
        types: [python]

# =============================================================================
# Global Settings
# =============================================================================

default_language_version:
  python: python

# Files to always exclude from all hooks
exclude: |
  (?x)^(
    archive/|
    legacy/|
    experimental/|
    \.git/|
    __pycache__/|
    \.mypy_cache/|
    \.ruff_cache/|
    \.pytest_cache/|
    node_modules/|
    \.venv/|
    venv/|
    build/|
    dist/|
    .*\.egg-info/
  )

# Don't fail fast - run all hooks
fail_fast: false

# CI configuration (for pre-commit.ci if used)
ci:
  autofix_commit_msg: "style: auto-fix from pre-commit hooks"
  autofix_prs: true
  autoupdate_branch: "main"
  autoupdate_commit_msg: "chore: update pre-commit hooks"
  skip: [pytest-unit, bandit, mypy] # Skip slow hooks in CI
